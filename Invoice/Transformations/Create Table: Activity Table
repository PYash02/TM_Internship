DROP TABLE IF EXISTS _INVOICE_ACTIVITIES; CREATE TABLE _INVOICE_ACTIVITIES (

_CASE_KEY INT

, _ACTIVITY_EN VARCHAR(300)

, _EVENTTIME TIMESTAMP

, _COUNTRY VARCHAR(100)

, _TEAM VARCHAR(100)

, _USER VARCHAR(100)

, _SORTING INT

);

SELECT COUNT(DISTINCT(_CASE_KEY)) FROM _INVOICE_ACTIVITIES;
